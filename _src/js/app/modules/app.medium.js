(function ($, window, document, undefined) {

	App.medium = (function () {

		var elements = {
		};

		var settings = {
		};

		function init() {
		}

		function destroy() {
		}

		// Reveal public methods
		return {
			init    : init,
			destroy : destroy
		};
	}());

}(jQuery, this, this.document));
